'use strict';
app.controller('memberDetailsController', [
	  '$modal'
	, '$scope'
	, '$timeout'
	, 'membersService'
	, 'formatHelpers'
	, (function (
	  $modal
	, $scope
	, $timeout
	, membersService
	, formatHelpers) {

	})
]);